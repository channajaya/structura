# Documentation only. Do not place secret values in this file.
# Store these values in protected GitHub/Vercel environment settings.

VERCEL_ORG_ID=
VERCEL_PROJECT_ID=

# VERCEL_TOKEN is intentionally not represented as a local value. It belongs
# only in the protected deployment environment's secret store.
